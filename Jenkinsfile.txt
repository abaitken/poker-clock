pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                script {
                  sshPublisher(publishers: [sshPublisherDesc(configName: 'pkgsrv', transfers: [sshTransfer(sourceFiles: '*.rpm')])])
                }

                sshPublisher {
                    publishers: [
                        sshPublisherDesc(
                            configName: 'Personal',
                            transfers: [
                                sshTransfer(
                                    sourceFiles: '**/*.zip',
                                    excludes: '**/nbproject/**',
                                    remoteDirectory: '/abaitken/stuff/PokerClockJs',
                                    removePrefix: ''
                                    remoteDirectorySDF: ''
                                    flatten: false,
                                    cleanRemote: false,
                                    noDefaultExcludes: false,
                                    makeEmptyDirs: false,
                                    patternSeparator: '[, ]+'
                                )
                            ]
                        )
                    ]                    
                }
            }
        }
    }
}